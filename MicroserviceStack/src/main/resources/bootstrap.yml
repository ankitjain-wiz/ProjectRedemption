spring:
  application.name: microservice-notification
  cloud:
    config:
      server:
        git:
          uri: file://D:/Ankit jain Work/LEARNING/ksessions/ProjectRainbow/testing
         
    
    consul:
      config:
        enabled: false

  autoconfigure:
    exclude : org.springframework.boot.autoconfigure.security.SecurityAutoConfiguration  
    
#Server port
server:
  port : 8888
  
  
 

    